#!/usr/bin/env jruby
require 'qtjruby-core'

# Load the application Gem path if it exists
gem_path = Qt::JRuby.root / 'gems'
if File.exist? gem_path
  Gem.clear_paths
  Gem.path.unshift(gem_path)
end

require ARGV.shift